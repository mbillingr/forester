/*
use tree::{Tree, TreeBuilder};

struct ExtraTreesRegressor {

}

struct ExtraTreesRegressorBuilder<X, Y> {
    n_trees: usize,
}


impl<X, Y> ExtraTreesRegressorBuilder<X, Y> {
    fn fit(&self, x: &[X], y: &[Y]) -> ExtraTreesRegressor<X, Y> {
        let trees = Vec::with_capacity(self.n_trees);
        for _ in 0..self.n_trees {
            trees.push(self.treebuilder.fit)
        }
    }
}
*/